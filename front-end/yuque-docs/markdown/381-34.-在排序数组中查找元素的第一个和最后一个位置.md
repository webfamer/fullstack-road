# 34. 在排序数组中查找元素的第一个和最后一个位置

> 来源：https://www.yuque.com/xiumubai/doc/ga96gsp6yzzn4rz1

1. 题目描述
给定一个按照升序排列的整数数组 nums，和一个目标值 target。找出给定目标值在数组中的开始位置和结束位置。

你的算法时间复杂度必须是 O(log n) 级别。

如果数组中不存在目标值，返回 [-1, -1]。

示例 1:
​JavaScriptRun CodeCopy912输入: nums = [5,7,7,8,8,10], target = 8输出: [3,4]示例 2:
​JavaScriptRun CodeCopy912输入: nums = [5,7,7,8,8,10], target = 6输出: [-1,-1]2. 实现思路
这个题目中要求时间复杂度为O(log n) 级别。所以，我们可以使用二分法查找到一个target值的位置，然后从这个值开始分别往前和后进行遍历，找到目标值的左右边界。
3. 代码实现
​JavaScriptRun CodeCopy99123456789101112131415161718192021/** * @param {number[]} nums * @param {number} target * @return {number[]} */var searchRange = function(nums, target) {    let left = 0, right = nums.length, mid	// 使用二分法确定mid和target相等时的位置    while(left <= right){        mid = (left + right) >> 1  // 取在平均值，向下取整。>>1相当于Math.floor()        if(nums[mid] === target) break        nums[mid] > target ? right = mid - 1 : left = mid + 1    }
    if(left > right) return [-1 , -1]	// 初始化i和j，用于从mid开始分别往左右遍历，确定左右边界    let i = mid, j = mid    while(nums[i] === nums[i - 1]) i--    while(nums[j] === nums[j + 1]) j++    return [i, j]};4. 提交结果
![](https://cdn.nlark.com/yuque/0/2021/png/1500604/1621584731643-8a6cba79-7caa-4d66-ac26-e33cb80a867b.png)
